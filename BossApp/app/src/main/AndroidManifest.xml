<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BossApp"
        tools:targetApi="31">

        <!-- LOGIN ACTIVITY - LAUNCHER (first screen) -->
        <activity
            android:name=".presentation.auth.LoginActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"
            android:theme="@style/Theme.BossApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MAIN ACTIVITY -->
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"
            android:theme="@style/Theme.BossApp" />

        <!-- REGISTER ACTIVITY -->
        <activity
            android:name=".presentation.auth.RegisterActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"
            android:theme="@style/Theme.BossApp" />

        <activity
            android:name=".presentation.profile.ChangePasswordActivity"
            android:theme="@style/Theme.BossApp"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait"/>

        <!-- QR Scanner Activity -->
        <activity
            android:name=".presentation.friends.QRScannerActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.BossApp" />

        <activity
            android:name=".presentation.task.TaskDetailActivity"
            android:theme="@style/Theme.BossApp"
            android:windowSoftInputMode="adjustResize"/>

        <activity android:name=".presentation.task.TaskCalendarActivity"/>

        <activity
            android:name=".presentation.task.TaskInfoActivity"
            android:theme="@style/Theme.BossApp"
            android:windowSoftInputMode="adjustResize"/>

        <activity
            android:name=".presentation.category.CategoryActivity"
            android:exported="false"
            android:label="Kategorije" />

        <activity android:name=".presentation.boss.BossFightActivity"
            android:exported="true" />



    </application>

</manifest>